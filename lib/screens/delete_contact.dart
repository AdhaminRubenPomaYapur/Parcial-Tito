import 'package:flutter/material.dart';

class DeleteContact extends StatelessWidget {

  const DeleteContact({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DeleteContact'),
      ),
    );
  }
}