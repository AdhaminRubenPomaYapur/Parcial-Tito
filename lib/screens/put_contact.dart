import 'package:flutter/material.dart';

class PutContact extends StatelessWidget {

  const PutContact({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PutContact'),
      ),
    );
  }
}